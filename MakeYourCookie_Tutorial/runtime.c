#include <stdio.h>
#include <stdlib.h>

// This is a minimal runtime file that doesn't need any special functions
// since we're only doing integer operations and using printf directly in the generated code 